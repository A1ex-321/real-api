<?php

namespace App\Models;

return [
    // 'dynamic_setting' => env('DYNAMIC_SETTING', 'default_value'),
    // // Example: Read a value from the database
    // $dynamicValue = smtp::pluck('MAIL_MAILER')->first(),

    // // Set the value in the configuration
    // config(['emailconfig.dynamic_setting' => $dynamicValue]),
    // config('emailconfig.dynamic_setting')

    // // 'dynamic_setting' => env('DYNAMIC_SETTING', 'default_value'),

];
